# firmware version refers to the SDK version for the platform
# We use the same SDK version as nCompass for compatibility
EGM_FIRMWARE_VERSION=$(< egm-firmware-version)
echo "EGM_FIRMWARE_VERSION=$EGM_FIRMWARE_VERSION"
